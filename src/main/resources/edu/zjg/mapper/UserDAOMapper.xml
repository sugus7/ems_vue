<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="edu.zjg.dao.UserDAO">


    <insert id="save" parameterType="edu.zjg.entity.User" useGeneratedKeys="true" keyProperty="id">
        insert into t_user values (#{id},#{username},#{realname},#{password},#{sex},#{status},#{registerTime})
    </insert>
    <!--查询一个用户名是否存在-->
    <select id="findByUsername" parameterType="String" resultType="edu.zjg.entity.User">
              select id,username,realname,password,sex,status,regsterTime
        from t_user where username=#{username}
    </select>
</mapper>